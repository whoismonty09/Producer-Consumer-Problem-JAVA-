# Producer-Consumer-Problem-JAVA-
This project demonstrates the classic Producer-Consumer problem using Java multithreading and synchronization. A shared buffer is used where the producer thread generates data and the consumer thread consumes it safely using inter-thread communication.
